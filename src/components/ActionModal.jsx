export function ActionModal({ title, fields, values, onChange, onClose, onSubmit, submitLabel = 'Save Changes' }) {
  return <div className="modal-backdrop" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><div className="modal-card" role="dialog" aria-modal="true" aria-labelledby="modal-title"><div className="modal-heading"><h2 id="modal-title">{title}</h2><button className="icon-button" onClick={onClose} aria-label="Close dialog"><i className="bi bi-x-lg" /></button></div><div className="modal-form">{fields.map((field) => <label key={field.key}>{field.label}{field.type === 'select' ? <select value={values[field.key] ?? ''} onChange={(event) => onChange(field.key, event.target.value)}>{field.options.map((option) => <option key={option} value={option}>{option}</option>)}</select> : <input autoFocus={field.key === fields[0].key} type={field.type || 'text'} value={values[field.key] ?? ''} placeholder={field.placeholder} onChange={(event) => onChange(field.key, event.target.value)} />}</label>)}</div><div className="modal-actions"><button className="outline-button" onClick={onClose}>Cancel</button><button className="primary-button" onClick={onSubmit}>{submitLabel}</button></div></div></div>;
}
